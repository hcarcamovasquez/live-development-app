{
  "name": "live-development-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter server dev",
    "build": "pnpm --filter web build && pnpm --filter server build",
    "start": "pnpm --filter server start",
    "lint": "pnpm --filter web lint"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
